# CSP (nonce) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Wdrożyć egzekwowany, nonce-owy Content-Security-Policy przez proxy Next 16, z mocnym `script-src`, bez łamania motywu / JSON-LD / obrazków / uploadu w adminie.

**Architecture:** Czysty builder stringa CSP (`app/_lib/csp.ts`, testowany). Proxy (`updateSession`) generuje nonce per request, ustawia `x-nonce` na request-headers i nagłówek `Content-Security-Policy` na zwracanych odpowiedziach renderujących HTML. Root layout i karta produktu czytają `x-nonce` przez `headers()` i przekazują go do dwóch inline-skryptów (next-themes, JSON-LD); resztę nonce'uje Next automatycznie.

**Tech Stack:** Next.js 16 App Router (Proxy), React 19, next-themes, Vitest.

## Global Constraints

- **Nonce = mechanizm z dokumentacji zainstalowanego Next 16**: generowany w proxy, czytany przez `(await headers()).get("x-nonce")` w Server Components. Nonce wymusza render dynamiczny — świadomie zaakceptowane (storefront i tak dynamiczny).
- **`script-src` bez `'unsafe-inline'`**: `'self' 'nonce-<n>' 'strict-dynamic'`, `'unsafe-eval'` TYLKO w dev (`NODE_ENV !== "production"`). Brak obcych origin skryptów.
- **`style-src 'self' 'unsafe-inline'`, BEZ nonce** — nonce w `style-src` sprawiłby, że CSP3 ignoruje `'unsafe-inline'` i łamie inline `style`.
- **`worker-src 'self' blob:`** (upload w adminie — worker z blob:). **`frame-ancestors 'none'`**, `object-src 'none'`, `base-uri 'self'`, `form-action 'self'`, `frame-src 'none'`, `upgrade-insecure-requests`.
- **Origin Supabase z `NEXT_PUBLIC_SUPABASE_URL`** (https + wss), bez hardkodowania. Unsplash `https://images.unsplash.com` w `img-src`. **Brak origin Stripe** (redirect server-side).
- **Egzekwująco** (`Content-Security-Policy`, nie Report-Only).
- **Sanitizer bez zmian** (poza poprawką komentarza „Bez img"); NIE dodajemy `sanitize-html`.
- **Testy jednostkowe tylko dla `buildCsp`**; proxy/plumbing → lint + build + ręczny smoke (wzorzec repo).
- **Commity: celowany `git add <ścieżki>`** — NIGDY `git add -A`. Gałąź `feat/csp-nonce` (spec zacommitowany). Copy po polsku. Trailer `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

## Struktura plików

- **Nowy:** `app/_lib/csp.ts` (`buildCsp`); `app/_lib/__tests__/csp.test.ts`.
- **Edycja:** `app/_components/layout/ThemeProvider.tsx` (prop `nonce`); `app/layout.tsx` (odczyt nonce → ThemeProvider); `app/produkt/[id]/page.tsx` (nonce na JSON-LD); `app/_lib/product-html.ts` (komentarz); `app/_lib/supabase/middleware.ts` (nonce + CSP w proxy).

---

### Task 1: Czysty builder CSP (`app/_lib/csp.ts`)

**Files:**
- Create: `app/_lib/csp.ts`
- Test: `app/_lib/__tests__/csp.test.ts`

**Interfaces:**
- Produces: `buildCsp(nonce: string, opts: { isDev: boolean; supabaseOrigin: string | null }): string`.

- [ ] **Step 1: Write the failing test**

Create `app/_lib/__tests__/csp.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { buildCsp } from "@/app/_lib/csp";

// Parsuje string CSP na mapę dyrektywa → tokeny.
function parse(csp: string): Record<string, string[]> {
  const out: Record<string, string[]> = {};
  for (const part of csp.split(";").map((p) => p.trim()).filter(Boolean)) {
    const [name, ...tokens] = part.split(/\s+/);
    out[name] = tokens;
  }
  return out;
}

const SB = "https://abc.supabase.co";

describe("buildCsp", () => {
  it("wstrzykuje nonce do script-src z 'strict-dynamic', bez 'unsafe-inline'", () => {
    const d = parse(buildCsp("N0nce", { isDev: false, supabaseOrigin: SB }));
    expect(d["script-src"]).toContain("'self'");
    expect(d["script-src"]).toContain("'nonce-N0nce'");
    expect(d["script-src"]).toContain("'strict-dynamic'");
    expect(d["script-src"]).not.toContain("'unsafe-inline'");
  });

  it("'unsafe-eval' tylko w dev", () => {
    expect(parse(buildCsp("n", { isDev: true, supabaseOrigin: SB }))["script-src"]).toContain("'unsafe-eval'");
    expect(parse(buildCsp("n", { isDev: false, supabaseOrigin: SB }))["script-src"]).not.toContain("'unsafe-eval'");
  });

  it("style-src ma 'unsafe-inline' i NIE ma nonce", () => {
    const d = parse(buildCsp("N0nce", { isDev: false, supabaseOrigin: SB }));
    expect(d["style-src"]).toContain("'unsafe-inline'");
    expect(d["style-src"].join(" ")).not.toContain("nonce");
  });

  it("origin Supabase (https+wss) w img-src/connect-src gdy podany", () => {
    const d = parse(buildCsp("n", { isDev: false, supabaseOrigin: SB }));
    expect(d["img-src"]).toContain(SB);
    expect(d["img-src"]).toContain("https://images.unsplash.com");
    expect(d["connect-src"]).toContain(SB);
    expect(d["connect-src"]).toContain("wss://abc.supabase.co");
  });

  it("brak origin Supabase gdy null (tylko self/data/blob + unsplash w img)", () => {
    const d = parse(buildCsp("n", { isDev: false, supabaseOrigin: null }));
    expect(d["connect-src"]).toEqual(["'self'"]);
    expect(d["img-src"]).toEqual(["'self'", "data:", "blob:", "https://images.unsplash.com"]);
  });

  it("twarde dyrektywy obecne", () => {
    const csp = buildCsp("n", { isDev: false, supabaseOrigin: SB });
    const d = parse(csp);
    expect(d["worker-src"]).toEqual(["'self'", "blob:"]);
    expect(d["frame-ancestors"]).toEqual(["'none'"]);
    expect(d["object-src"]).toEqual(["'none'"]);
    expect(d["base-uri"]).toEqual(["'self'"]);
    expect(d["form-action"]).toEqual(["'self'"]);
    expect(d["default-src"]).toEqual(["'self'"]);
    expect(csp).toContain("upgrade-insecure-requests");
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run app/_lib/__tests__/csp.test.ts`
Expected: FAIL — `Cannot find module '@/app/_lib/csp'`.

- [ ] **Step 3: Write the implementation**

Create `app/_lib/csp.ts`:

```ts
// Czysty builder nagłówka Content-Security-Policy — bez zależności server-only,
// testowalny bez proxy. Wołany z updateSession (proxy Next 16) per request.
// Uwaga: 'unsafe-inline' jest TYLKO w style-src (inline style={{}} + style= z
// sanitizera; nonce nie obejmuje atrybutu style). script-src NIE ma
// 'unsafe-inline' — tam liczy się ochrona przed XSS.

type CspOpts = { isDev: boolean; supabaseOrigin: string | null };

export function buildCsp(nonce: string, { isDev, supabaseOrigin }: CspOpts): string {
  const sbHttps = supabaseOrigin ? [supabaseOrigin] : [];
  const sbWss = supabaseOrigin ? [supabaseOrigin.replace(/^https:/, "wss:")] : [];

  const directives: Record<string, string[]> = {
    "default-src": ["'self'"],
    "script-src": [
      "'self'",
      `'nonce-${nonce}'`,
      "'strict-dynamic'",
      ...(isDev ? ["'unsafe-eval'"] : []),
    ],
    "style-src": ["'self'", "'unsafe-inline'"],
    "img-src": ["'self'", "data:", "blob:", ...sbHttps, "https://images.unsplash.com"],
    "font-src": ["'self'"],
    "connect-src": ["'self'", ...sbHttps, ...sbWss],
    "worker-src": ["'self'", "blob:"],
    "object-src": ["'none'"],
    "base-uri": ["'self'"],
    "form-action": ["'self'"],
    "frame-ancestors": ["'none'"],
    "frame-src": ["'none'"],
  };

  const parts = Object.entries(directives).map(([name, vals]) => `${name} ${vals.join(" ")}`);
  parts.push("upgrade-insecure-requests");
  return parts.join("; ");
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run app/_lib/__tests__/csp.test.ts`
Expected: PASS (all cases).

- [ ] **Step 5: Commit**

```bash
git add app/_lib/csp.ts app/_lib/__tests__/csp.test.ts
git commit -m "feat(bezpieczenstwo): czysty builder nagłówka CSP (buildCsp)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: Plumbing nonce do inline-skryptów (nieszkodliwe bez CSP)

**Files:**
- Modify: `app/_components/layout/ThemeProvider.tsx`
- Modify: `app/layout.tsx`
- Modify: `app/produkt/[id]/page.tsx`
- Modify: `app/_lib/product-html.ts`

**Interfaces:**
- Consumes: nic z Task 1.
- Produces: `ThemeProvider` przyjmuje opcjonalny prop `nonce?: string`.

Uwaga: bez CSP (Task 3 jeszcze nie wdrożone) `x-nonce` nie istnieje → `headers().get("x-nonce")` zwraca `null` → `nonce` = `undefined` → next-themes/JSON-LD renderują się jak dotąd (bez atrybutu nonce). Zmiana jest więc bezpieczna i nie łamie apki. (Odczyt `headers()` w layout czyni render dynamicznym — to docelowy, zaakceptowany stan.)

- [ ] **Step 1: `ThemeProvider` — prop `nonce`**

Zastąp całą zawartość `app/_components/layout/ThemeProvider.tsx`:

```tsx
"use client";

import { ThemeProvider as NextThemesProvider } from "next-themes";

export function ThemeProvider({
  children,
  nonce,
}: {
  children: React.ReactNode;
  nonce?: string;
}) {
  return (
    <NextThemesProvider attribute="class" defaultTheme="light" enableSystem nonce={nonce}>
      {children}
    </NextThemesProvider>
  );
}
```

- [ ] **Step 2: `layout.tsx` — odczyt nonce, przekazanie do ThemeProvider**

W `app/layout.tsx` dodaj import (po linii 1 `import type { Metadata } from "next";`):

```tsx
import { headers } from "next/headers";
```

W `RootLayout` (obecnie zaczyna się `const locale = await getLocale();`, linia 71) dodaj odczyt nonce jako pierwszą linię ciała funkcji:

```tsx
  const nonce = (await headers()).get("x-nonce") ?? undefined;
  const locale = await getLocale();
```

Zmień otwarcie `<ThemeProvider>` (linia 81) na:

```tsx
        <ThemeProvider nonce={nonce}>
```

- [ ] **Step 3: `produkt/[id]/page.tsx` — nonce na JSON-LD**

W `app/produkt/[id]/page.tsx` dodaj import `headers` (dołóż do istniejących importów u góry pliku):

```tsx
import { headers } from "next/headers";
```

Tuż po linii `const jsonLdHtml = JSON.stringify(jsonLd).replace(/</g, "\\u003c");` (linia 223) dodaj:

```tsx
  const nonce = (await headers()).get("x-nonce") ?? undefined;
```

Zmień znacznik JSON-LD (linie 245-248) na (dodany `nonce`):

```tsx
      <script
        type="application/ld+json"
        nonce={nonce}
        dangerouslySetInnerHTML={{ __html: jsonLdHtml }}
      />
```

- [ ] **Step 4: `product-html.ts` — poprawka nieaktualnego komentarza**

W `app/_lib/product-html.ts` zmień linię 8:

```
// listy, podkreślenia, nagłówki H2-H4 i linki. Bez img/video/script/iframe.
```
na:
```
// listy, podkreślenia, nagłówki H2-H4, linki i img. Bez video/script/iframe/style-taga.
```

- [ ] **Step 5: Weryfikacja lint + build**

Run: `npm run lint && npm run build`
Expected: lint czysty; build EXIT 0. (Aplikacja działa; nonce jest `undefined` do czasu Task 3.)

- [ ] **Step 6: Commit**

```bash
git add "app/_components/layout/ThemeProvider.tsx" app/layout.tsx "app/produkt/[id]/page.tsx" app/_lib/product-html.ts
git commit -m "feat(bezpieczenstwo): przekazanie nonce do next-themes i JSON-LD

Odczyt x-nonce (headers) w layout i na karcie produktu; ThemeProvider dostaje
prop nonce. Nieszkodliwe bez CSP (nonce=undefined). Poprawka komentarza sanitizera.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: Włączenie CSP w proxy (`updateSession`)

**Files:**
- Modify: `app/_lib/supabase/middleware.ts`

**Interfaces:**
- Consumes: `buildCsp` (Task 1); `x-nonce` czytany przez layout/kartę produktu (Task 2).

- [ ] **Step 1: Import buildCsp**

W `app/_lib/supabase/middleware.ts` dodaj po linii 3 (`import { localizePath, stripLocale } from "../i18n";`):

```ts
import { buildCsp } from "../csp";
```

- [ ] **Step 2: Nonce + string CSP (obok x-locale)**

Zmień blok (obecnie linie 12-13):

```ts
  const requestHeaders = new Headers(request.headers);
  requestHeaders.set("x-locale", locale);
```
na:

```ts
  const requestHeaders = new Headers(request.headers);
  requestHeaders.set("x-locale", locale);

  // Nonce CSP — świeży per request; x-nonce czytają server components (layout,
  // karta produktu) i doklejają go do inline-skryptów (next-themes, JSON-LD).
  const nonce = Buffer.from(crypto.randomUUID()).toString("base64");
  requestHeaders.set("x-nonce", nonce);
  let supabaseOrigin: string | null = null;
  try {
    supabaseOrigin = process.env.NEXT_PUBLIC_SUPABASE_URL
      ? new URL(process.env.NEXT_PUBLIC_SUPABASE_URL).origin
      : null;
  } catch {
    supabaseOrigin = null;
  }
  const csp = buildCsp(nonce, {
    isDev: process.env.NODE_ENV !== "production",
    supabaseOrigin,
  });
```

- [ ] **Step 3: Ustaw nagłówek CSP na zwracanych odpowiedziach renderujących HTML**

W tej samej funkcji ustaw `Content-Security-Policy` na obu odpowiedziach, które renderują HTML (redirecty pomijamy — nie mają treści).

Zmień blok DE-rewrite return (obecnie linie 98-101):

```ts
    supabaseResponse.cookies.getAll().forEach((cookie) => {
      rewriteResponse.cookies.set(cookie);
    });
    return rewriteResponse;
```
na:

```ts
    supabaseResponse.cookies.getAll().forEach((cookie) => {
      rewriteResponse.cookies.set(cookie);
    });
    rewriteResponse.headers.set("Content-Security-Policy", csp);
    return rewriteResponse;
```

Zmień końcowy return (obecnie linia 104 `return supabaseResponse;`) na:

```ts
  supabaseResponse.headers.set("Content-Security-Policy", csp);
  return supabaseResponse;
```

- [ ] **Step 4: Weryfikacja lint + build**

Run: `npm run lint && npm run build`
Expected: lint czysty; build EXIT 0.

- [ ] **Step 5: Smoke test runtime (deferowany — wykonuje kontroler/użytkownik)**

Manualnie (lub kontroler przez `npm start` + curl): nagłówek `Content-Security-Policy` obecny na `/`, `/sklep`, `/produkt/{id}`, `/de`; w HTML `<script nonce="...">` na skrypcie next-themes i JSON-LD; w przeglądarce **brak naruszeń CSP w konsoli**; motyw przełącza się bez migania; obrazki (Supabase/Unsplash) ładują się; **upload zdjęcia w adminie działa** (worker blob). W razie problemu: chwilowo zamienić `"Content-Security-Policy"` na `"Content-Security-Policy-Report-Only"` i obserwować.

- [ ] **Step 6: Commit**

```bash
git add app/_lib/supabase/middleware.ts
git commit -m "feat(bezpieczenstwo): egzekwowany CSP z nonce w proxy

Nonce per request w updateSession; nagłówek Content-Security-Policy (buildCsp)
na odpowiedziach renderujących HTML (PL next + DE rewrite). script-src mocny
(nonce+strict-dynamic, bez unsafe-inline), origin Supabase z env.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review (autor planu)

**1. Pokrycie spec:**
- Nonce w proxy/updateSession + x-nonce + CSP na każdej renderującej odpowiedzi → Task 3. ✅
- Dokładna polityka (script-src bez unsafe-inline; style-src unsafe-inline bez nonce; worker-src blob:; frame-ancestors none; upgrade-insecure-requests; origin Supabase z env https+wss; Unsplash; brak Stripe) → Task 1 (`buildCsp`) + testy. ✅
- Plumbing nonce: next-themes (layout + ThemeProvider), JSON-LD (karta produktu) → Task 2. ✅
- Render dynamiczny jako konsekwencja odczytu headers() → Task 2 nota + Global Constraints. ✅
- Sanitizer bez zmian poza komentarzem → Task 2 Step 4; brak `sanitize-html`. ✅
- Egzekwująco (nie report-only), z report-only jako fallback → Task 3 Step 5. ✅
- 'unsafe-eval' tylko w dev → `buildCsp` + test. ✅
- Testy tylko dla buildCsp; reszta lint/build/smoke → Task 1 testy; Task 2/3 lint+build+smoke. ✅

**2. Placeholdery:** brak „TBD/TODO/handle edge cases" — każdy krok ma realny kod/komendę. ✅

**3. Spójność typów:** `buildCsp(nonce, { isDev, supabaseOrigin })` identyczna w Task 1 (definicja+test) i Task 3 (wywołanie); `ThemeProvider` prop `nonce?: string` spójny między definicją (Task 2 Step 1) a użyciem (Task 2 Step 2); `x-nonce` ustawiane w Task 3, czytane w Task 2 (ta sama nazwa nagłówka). Kolejność Task 2 przed Task 3 celowa (plumbing nieszkodliwy bez nagłówka). ✅
