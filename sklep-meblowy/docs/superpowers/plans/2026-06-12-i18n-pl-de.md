# Dwujęzyczność PL/DE (etap ①) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sklep dwujęzyczny PL (korzeń) + DE (`/de`) z auto-tłumaczeniem treści (DeepL Free), fallbackiem do PL i lokalnym SEO — etap ①/4 programu „Sprzedaż w Niemczech".

**Architecture:** `proxy.ts` (Next 16, d. middleware) przepisuje `/de/*`→`/*` i wstrzykuje nagłówek `x-locale`; server components czytają locale przez `headers()`. Tłumaczenia trzymane w kolumnach `_de` + flaga `needs_translation`; pipeline DeepL działa inline (panel) i przez sweep-cron (sync hurtowy). Odczyt wybiera pole `_de` z fallbackiem PL. SEO: hreflang/sitemap obejmują DE tylko dla przetłumaczonych.

**Tech Stack:** Next 16 (App Router; `proxy.ts`, `headers()` async, `MetadataRoute.Sitemap` z `alternates.languages`), Supabase (service role), DeepL API Free, vitest (pure helpery).

**Konwencje repo:** kod w `sklep-meblowy/`; testy w `app/_lib/__tests__/*.test.ts` (vitest, alias `@/`); komendy z katalogu `sklep-meblowy/` (`npm test`, `node_modules\.bin\tsc.cmd --noEmit`, `npm run lint`); sekrety przez `safeCompareSecret` (`app/_lib/secure-compare.ts`); akcje admina przez `requireAdmin` + `createAdminClient`. Push: konto gh `Woodecky10`.

**Poza zakresem (etapy ②③④):** ceny EUR, niemieckie strony prawne (Impressum/Widerruf), VAT/OSS. Ceny zostają w PLN (formatowane wg locale).

---

## Faza A — Fundament (locale + routing + schema)

### Task 1: i18n core — stałe i pure-helpery

**Files:**
- Create: `sklep-meblowy/app/_lib/i18n.ts`
- Test: `sklep-meblowy/app/_lib/__tests__/i18n.test.ts`

- [ ] **Step 1: Test (pure helpery)**

```ts
// app/_lib/__tests__/i18n.test.ts
import { describe, it, expect } from "vitest";
import { stripLocale, localizePath, pickLocalized, isLocale } from "@/app/_lib/i18n";

describe("i18n helpery", () => {
  it("isLocale", () => {
    expect(isLocale("de")).toBe(true);
    expect(isLocale("pl")).toBe(true);
    expect(isLocale("fr")).toBe(false);
  });
  it("stripLocale — /de prefix", () => {
    expect(stripLocale("/de/sklep")).toEqual({ locale: "de", pathname: "/sklep" });
    expect(stripLocale("/de")).toEqual({ locale: "de", pathname: "/" });
    expect(stripLocale("/sklep")).toEqual({ locale: "pl", pathname: "/sklep" });
    expect(stripLocale("/")).toEqual({ locale: "pl", pathname: "/" });
    // nie myli prefiksu z fragmentem słowa
    expect(stripLocale("/depilacja")).toEqual({ locale: "pl", pathname: "/depilacja" });
  });
  it("localizePath", () => {
    expect(localizePath("/sklep", "de")).toBe("/de/sklep");
    expect(localizePath("/", "de")).toBe("/de");
    expect(localizePath("/sklep", "pl")).toBe("/sklep");
  });
  it("pickLocalized — DE z fallbackiem PL", () => {
    expect(pickLocalized("Sofa", "Couch", "de")).toBe("Couch");
    expect(pickLocalized("Sofa", null, "de")).toBe("Sofa");
    expect(pickLocalized("Sofa", "", "de")).toBe("Sofa");
    expect(pickLocalized("Sofa", "Couch", "pl")).toBe("Sofa");
  });
});
```

- [ ] **Step 2: Uruchom — ma FAIL**

Run: `cd sklep-meblowy; npm test -- i18n`
Expected: FAIL (moduł nie istnieje).

- [ ] **Step 3: Implementacja**

```ts
// app/_lib/i18n.ts
export const LOCALES = ["pl", "de"] as const;
export type Locale = (typeof LOCALES)[number];
export const DEFAULT_LOCALE: Locale = "pl";

export function isLocale(v: string): v is Locale {
  return (LOCALES as readonly string[]).includes(v);
}

// Rozbija ścieżkę na { locale, pathname-bez-prefiksu }. Tylko '/de' jest prefiksem
// (PL = korzeń, bez prefiksu). Dopasowuje '/de' albo '/de/...', nie '/depilacja'.
export function stripLocale(pathname: string): { locale: Locale; pathname: string } {
  if (pathname === "/de") return { locale: "de", pathname: "/" };
  if (pathname.startsWith("/de/")) return { locale: "de", pathname: pathname.slice(3) };
  return { locale: DEFAULT_LOCALE, pathname };
}

// Dokleja prefiks locale do ścieżki (PL bez prefiksu).
export function localizePath(pathname: string, locale: Locale): string {
  if (locale === DEFAULT_LOCALE) return pathname;
  return pathname === "/" ? "/de" : `/de${pathname}`;
}

// Wybiera wartość DE (z fallbackiem do PL gdy pusta) albo PL.
export function pickLocalized(
  pl: string,
  de: string | null | undefined,
  locale: Locale
): string {
  if (locale === "de" && de && de.trim().length > 0) return de;
  return pl;
}
```

- [ ] **Step 4: Uruchom — ma PASS**

Run: `cd sklep-meblowy; npm test -- i18n`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add sklep-meblowy/app/_lib/i18n.ts sklep-meblowy/app/_lib/__tests__/i18n.test.ts
git commit -m "feat(i18n): stałe locale + pure-helpery (stripLocale/localizePath/pickLocalized)"
```

---

### Task 2: `getLocale()` server-side + `proxy.ts` (rewrite /de + x-locale)

**Files:**
- Modify: `sklep-meblowy/app/_lib/i18n.ts` (dodaj `getLocale`)
- Create: `sklep-meblowy/proxy.ts`

- [ ] **Step 1: Dodaj `getLocale()` do i18n.ts** (czyta nagłówek wstrzyknięty przez proxy)

```ts
// dopisać na końcu app/_lib/i18n.ts
import { headers } from "next/headers";

// Server-only: locale bieżącego żądania. Proxy wstrzykuje nagłówek 'x-locale'.
// headers() w Next 16 jest async.
export async function getLocale(): Promise<Locale> {
  const h = await headers();
  const v = h.get("x-locale");
  return v && isLocale(v) ? v : DEFAULT_LOCALE;
}
```

- [ ] **Step 2: Utwórz `proxy.ts`** (Next 16 — d. middleware; rewrite `/de/*`→`/*` + nagłówek)

```ts
// sklep-meblowy/proxy.ts  (root, obok app/)
import { NextResponse } from "next/server";
import type { NextRequest } from "next/server";
import { stripLocale } from "@/app/_lib/i18n";

export function proxy(request: NextRequest) {
  const { locale, pathname } = stripLocale(request.nextUrl.pathname);

  // Tylko ścieżki /de wymagają rewrite + ustawienia locale. PL idzie bez zmian.
  const requestHeaders = new Headers(request.headers);
  requestHeaders.set("x-locale", locale);

  if (locale === "de") {
    const url = request.nextUrl.clone();
    url.pathname = pathname; // /de/sklep -> /sklep (URL w przeglądarce zostaje /de/sklep)
    return NextResponse.rewrite(url, { request: { headers: requestHeaders } });
  }

  return NextResponse.next({ request: { headers: requestHeaders } });
}

export const config = {
  // Pomijamy interne Next, API, pliki statyczne, sitemap/robots (mają własne locale-handling).
  matcher: ["/((?!_next/|api/|.*\\..*|sitemap\\.xml|robots\\.txt).*)"],
};
```

- [ ] **Step 3: Typecheck + lint**

Run: `cd sklep-meblowy; node_modules\.bin\tsc.cmd --noEmit -p tsconfig.json; npm run lint`
Expected: exit 0 (oba).

- [ ] **Step 4: Smoke-test ręczny**

Run: `cd sklep-meblowy; npm run dev` → otwórz `http://localhost:3000/de/sklep`.
Expected: strona `/sklep` renderuje się pod URL `/de/sklep` (treść na razie PL — render-locale dochodzi w Fazie C). `http://localhost:3000/sklep` działa bez zmian.

> Jeśli proxy nie odpala: sprawdź `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md` (matcher / nazwa eksportu). NIE używać `middleware.ts` — w Next 16 to `proxy.ts`.

- [ ] **Step 5: Commit**

```bash
git add sklep-meblowy/app/_lib/i18n.ts sklep-meblowy/proxy.ts
git commit -m "feat(i18n): proxy.ts rewrite /de + x-locale, getLocale() server"
```

---

### Task 3: Migracja 29 — kolumny `_de` + `needs_translation`

**Files:**
- Create: `sklep-meblowy/supabase/migrations/29_i18n_de_columns.sql`

- [ ] **Step 1: Napisz migrację**

```sql
-- ============================================================
-- Migracja 29: kolumny tłumaczeń DE + flaga świeżości (i18n etap ①)
-- Uruchom w Supabase SQL Editor.
-- ============================================================
-- Kolumny _de nullable; brak/pusto = fallback do PL przy odczycie.
-- needs_translation: true = do (re)tłumaczenia przez sweep; czyszczone po zapisie DE
-- (auto lub ręczna korekta w panelu).

alter table public.products
  add column if not exists name_de text,
  add column if not exists description_de text,
  add column if not exists description_sections_de jsonb,
  add column if not exists color_de text,
  add column if not exists material_de text,
  add column if not exists needs_translation boolean not null default true,
  add column if not exists translated_at timestamptz;

alter table public.categories
  add column if not exists label_de text,
  add column if not exists needs_translation boolean not null default true,
  add column if not exists translated_at timestamptz;

alter table public.category_groups
  add column if not exists label_de text,
  add column if not exists needs_translation boolean not null default true,
  add column if not exists translated_at timestamptz;

alter table public.product_reviews
  add column if not exists comment_de text,
  add column if not exists needs_translation boolean not null default true,
  add column if not exists translated_at timestamptz;

-- Indeks częściowy — sweep szybko znajduje zaległe.
create index if not exists idx_products_needs_translation
  on public.products (needs_translation) where needs_translation = true;
create index if not exists idx_reviews_needs_translation
  on public.product_reviews (needs_translation) where needs_translation = true;

-- RLS: kolumny dziedziczą polityki tabel (odczyt publiczny products/categories,
-- write tylko service role po migracji 26). Brak nowych polityk.
```

- [ ] **Step 2: Zaktualizuj baseline `supabase/schema.sql`** — dopisz te kolumny do `create table products / categories / category_groups / product_reviews` (zgodnie z polityką jednego baseline'u z migracji #21). Dodaj `name_de text`, `description_de text`, `description_sections_de jsonb`, `color_de text`, `material_de text`, `needs_translation boolean not null default true`, `translated_at timestamptz` do `products`; `label_de`+flagi do `categories`/`category_groups`; `comment_de`+flagi do `product_reviews`.

- [ ] **Step 3: Commit** (migracja to SQL — brak testu jednostkowego, weryfikacja przy deployu)

```bash
git add sklep-meblowy/supabase/migrations/29_i18n_de_columns.sql sklep-meblowy/supabase/schema.sql
git commit -m "feat(db): migracja 29 — kolumny _de + needs_translation (i18n)"
```

---

## Faza B — Pipeline tłumaczeń

### Task 4: Klient DeepL (`translate.ts`)

**Files:**
- Create: `sklep-meblowy/app/_lib/translate.ts`
- Test: `sklep-meblowy/app/_lib/__tests__/translate.test.ts`

- [ ] **Step 1: Test (wstrzyknięty fetch)**

```ts
// app/_lib/__tests__/translate.test.ts
import { describe, it, expect, vi } from "vitest";
import { translateTexts } from "@/app/_lib/translate";

function fakeFetch(translations: string[]) {
  return vi.fn(async () =>
    new Response(JSON.stringify({ translations: translations.map((t) => ({ text: t })) }), {
      status: 200,
      headers: { "content-type": "application/json" },
    })
  );
}

describe("translateTexts (DeepL)", () => {
  it("buduje request PL→DE i parsuje odpowiedź", async () => {
    const f = fakeFetch(["Couch", "Bequem"]);
    const out = await translateTexts(["Sofa", "Wygodna"], {
      apiKey: "k", fetchImpl: f as unknown as typeof fetch,
    });
    expect(out).toEqual(["Couch", "Bequem"]);
    const [url, init] = f.mock.calls[0];
    expect(String(url)).toContain("api-free.deepl.com/v2/translate");
    expect((init as RequestInit).method).toBe("POST");
    const body = String((init as RequestInit).body);
    expect(body).toContain("source_lang=PL");
    expect(body).toContain("target_lang=DE");
    expect(body).toContain("text=Sofa");
  });
  it("html=true ustawia tag_handling", async () => {
    const f = fakeFetch(["<b>Couch</b>"]);
    await translateTexts(["<b>Sofa</b>"], { apiKey: "k", html: true, fetchImpl: f as unknown as typeof fetch });
    expect(String((f.mock.calls[0][1] as RequestInit).body)).toContain("tag_handling=html");
  });
  it("pusta lista → [] bez wywołania fetch", async () => {
    const f = vi.fn();
    expect(await translateTexts([], { apiKey: "k", fetchImpl: f as unknown as typeof fetch })).toEqual([]);
    expect(f).not.toHaveBeenCalled();
  });
  it("błąd HTTP → rzuca", async () => {
    const f = vi.fn(async () => new Response("nope", { status: 456 }));
    await expect(
      translateTexts(["x"], { apiKey: "k", fetchImpl: f as unknown as typeof fetch })
    ).rejects.toThrow();
  });
});
```

- [ ] **Step 2: Uruchom — FAIL**

Run: `cd sklep-meblowy; npm test -- translate`
Expected: FAIL.

- [ ] **Step 3: Implementacja**

```ts
// app/_lib/translate.ts
const DEEPL_URL = "https://api-free.deepl.com/v2/translate";

export type TranslateOpts = {
  apiKey?: string;            // domyślnie process.env.DEEPL_API_KEY
  html?: boolean;             // tag_handling=html dla treści z HTML
  fetchImpl?: typeof fetch;   // wstrzykiwany w testach
};

// PL→DE. Zwraca tłumaczenia w tej samej kolejności co wejście.
// Rzuca przy braku klucza / błędzie HTTP — caller łapie (best-effort).
export async function translateTexts(
  texts: string[],
  opts: TranslateOpts = {}
): Promise<string[]> {
  if (texts.length === 0) return [];
  const apiKey = opts.apiKey ?? process.env.DEEPL_API_KEY;
  if (!apiKey) throw new Error("DEEPL_API_KEY nie jest ustawiony");
  const doFetch = opts.fetchImpl ?? fetch;

  const params = new URLSearchParams();
  params.set("source_lang", "PL");
  params.set("target_lang", "DE");
  if (opts.html) params.set("tag_handling", "html");
  for (const t of texts) params.append("text", t);

  const res = await doFetch(DEEPL_URL, {
    method: "POST",
    headers: {
      Authorization: `DeepL-Auth-Key ${apiKey}`,
      "Content-Type": "application/x-www-form-urlencoded",
    },
    body: params.toString(),
  });
  if (!res.ok) throw new Error(`DeepL HTTP ${res.status}`);
  const data = (await res.json()) as { translations: { text: string }[] };
  return (data.translations ?? []).map((t) => t.text);
}
```

- [ ] **Step 4: Uruchom — PASS**

Run: `cd sklep-meblowy; npm test -- translate`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add sklep-meblowy/app/_lib/translate.ts sklep-meblowy/app/_lib/__tests__/translate.test.ts
git commit -m "feat(i18n): klient DeepL Free (HTML-aware, wstrzykiwany fetch) + testy"
```

---

### Task 5: Tłumacze encji (`translate-entities.ts`)

**Files:**
- Create: `sklep-meblowy/app/_lib/translate-entities.ts`
- Test: `sklep-meblowy/app/_lib/__tests__/translate-entities.test.ts`

Kontrakt: funkcje czyste — dostają obiekt PL + funkcję `translate(texts, {html}) => Promise<string[]>` (wstrzyknięty klient), zwracają pola `_de`. Sekcje opisu (`ProductDescriptionSection[]`) tłumaczą `title/body/admin_title/admin_body` sekcji `text` oraz `image_alt/caption` sekcji `image`; resztę (url, flagi) zostawiają.

- [ ] **Step 1: Test**

```ts
// app/_lib/__tests__/translate-entities.test.ts
import { describe, it, expect } from "vitest";
import { translateProductFields } from "@/app/_lib/translate-entities";

// fałszywy translate: echo "DE:"+tekst, zachowuje kolejność
const fake = async (texts: string[]) => texts.map((t) => `DE:${t}`);

describe("translateProductFields", () => {
  it("tłumaczy nazwę/opis/kolor/materiał + sekcje", async () => {
    const out = await translateProductFields(
      {
        name: "Sofa",
        description: "<p>Wygodna</p>",
        color: "beż",
        material: "welur",
        description_sections: [
          { kind: "text", title: "Materiał", body: "<b>Miękki</b>" },
          { kind: "image", image_url: "x.jpg", image_alt: "zdjęcie", caption: "podpis" },
        ],
      },
      fake
    );
    expect(out.name_de).toBe("DE:Sofa");
    expect(out.description_de).toBe("DE:<p>Wygodna</p>");
    expect(out.color_de).toBe("DE:beż");
    expect(out.material_de).toBe("DE:welur");
    expect(out.description_sections_de?.[0]).toMatchObject({ kind: "text", title: "DE:Materiał", body: "DE:<b>Miękki</b>" });
    expect(out.description_sections_de?.[1]).toMatchObject({ kind: "image", image_url: "x.jpg", image_alt: "DE:zdjęcie", caption: "DE:podpis" });
  });
  it("puste/null pola zostają puste (nie woła translate na pusto)", async () => {
    const out = await translateProductFields({ name: "Sofa", description: "", color: null, material: null, description_sections: null }, fake);
    expect(out.name_de).toBe("DE:Sofa");
    expect(out.description_de).toBe("");
    expect(out.color_de).toBeNull();
  });
});
```

- [ ] **Step 2: Uruchom — FAIL**

Run: `cd sklep-meblowy; npm test -- translate-entities`

- [ ] **Step 3: Implementacja**

```ts
// app/_lib/translate-entities.ts
import type { ProductDescriptionSection } from "./types";

export type TranslateFn = (texts: string[], opts?: { html?: boolean }) => Promise<string[]>;

type ProductPl = {
  name: string;
  description: string;
  color?: string | null;
  material?: string | null;
  description_sections?: ProductDescriptionSection[] | null;
};
type ProductDe = {
  name_de: string;
  description_de: string;
  color_de: string | null;
  material_de: string | null;
  description_sections_de: ProductDescriptionSection[] | null;
};

// Tłumaczy pojedynczy string przez batcha (zachowuje pusty input bez wywołania).
async function one(t: string | null | undefined, fn: TranslateFn, html = false): Promise<string | null> {
  const s = (t ?? "").trim();
  if (!s) return t == null ? null : "";
  return (await fn([s], { html }))[0];
}

export async function translateProductFields(p: ProductPl, fn: TranslateFn): Promise<ProductDe> {
  const [name_de, description_de, color_de, material_de] = await Promise.all([
    one(p.name, fn).then((v) => v ?? ""),
    one(p.description, fn, true).then((v) => v ?? ""),
    one(p.color, fn),
    one(p.material, fn),
  ]);

  let sections_de: ProductDescriptionSection[] | null = null;
  const secs = p.description_sections;
  if (secs && secs.length > 0) {
    sections_de = await Promise.all(
      secs.map(async (s) => {
        if (s.kind === "text") {
          return {
            ...s,
            title: (await one(s.title, fn)) ?? s.title,
            body: (await one(s.body, fn, true)) ?? s.body,
            admin_title: s.admin_title ? (await one(s.admin_title, fn)) ?? s.admin_title : s.admin_title,
            admin_body: s.admin_body ? (await one(s.admin_body, fn, true)) ?? s.admin_body : s.admin_body,
          } as ProductDescriptionSection;
        }
        return {
          ...s,
          image_alt: (await one(s.image_alt, fn)) ?? s.image_alt,
          caption: s.caption ? (await one(s.caption, fn)) ?? s.caption : s.caption,
        } as ProductDescriptionSection;
      })
    );
  }

  return { name_de, description_de, color_de, material_de, description_sections_de: sections_de };
}

// Analogiczne, prostsze:
export async function translateLabel(label: string, fn: TranslateFn): Promise<string> {
  return (await one(label, fn)) ?? "";
}
export async function translateComment(comment: string | null, fn: TranslateFn): Promise<string | null> {
  return one(comment, fn, false);
}
```

> Uwaga: dopasuj kształt `ProductDescriptionSection` do `app/_lib/types.ts` (pola `title/body/admin_title/admin_body/hidden/admin_custom` dla `text`; `image_url/image_alt/caption` dla `image`). Jeśli pole opcjonalne — `one()` zwróci je puste/null bez psucia.

- [ ] **Step 4: Uruchom — PASS** (`npm test -- translate-entities`)

- [ ] **Step 5: Commit**

```bash
git add sklep-meblowy/app/_lib/translate-entities.ts sklep-meblowy/app/_lib/__tests__/translate-entities.test.ts
git commit -m "feat(i18n): tłumacze encji (produkt/etykieta/recenzja) + testy"
```

---

### Task 6: Orkiestrator sweepa (`translation-sweep.ts`)

**Files:**
- Create: `sklep-meblowy/app/_lib/translation-sweep.ts`
- Test: `sklep-meblowy/app/_lib/__tests__/translation-sweep.test.ts`

Czysta orkiestracja (wzorzec jak `baselinker-reconcile.ts`): dostaje listę itemów + `translateOne(item)`, iteruje sekwencyjnie (limity DeepL), kategoryzuje, raportuje `backlog`.

- [ ] **Step 1: Test**

```ts
// app/_lib/__tests__/translation-sweep.test.ts
import { describe, it, expect } from "vitest";
import { runTranslationSweep } from "@/app/_lib/translation-sweep";

describe("runTranslationSweep", () => {
  it("kategoryzuje translated/failed i liczy", async () => {
    const items = [{ id: "a" }, { id: "b" }, { id: "c" }];
    const res = await runTranslationSweep(items, async (it) => {
      if (it.id === "b") throw new Error("DeepL down");
    });
    expect(res.translated).toBe(2);
    expect(res.failed).toBe(1);
    expect(res.scanned).toBe(3);
    expect(res.backlog).toBe(false);
  });
  it("backlog=true gdy itemów = limitowi (może być więcej)", async () => {
    const items = [{ id: "a" }, { id: "b" }];
    const res = await runTranslationSweep(items, async () => {}, { limitReached: true });
    expect(res.backlog).toBe(true);
  });
  it("błąd jednego itemu nie przerywa pętli", async () => {
    const seen: string[] = [];
    await runTranslationSweep([{ id: "a" }, { id: "b" }], async (it) => {
      seen.push(it.id);
      if (it.id === "a") throw new Error("x");
    });
    expect(seen).toEqual(["a", "b"]);
  });
});
```

- [ ] **Step 2: FAIL** (`npm test -- translation-sweep`)

- [ ] **Step 3: Implementacja**

```ts
// app/_lib/translation-sweep.ts
export type SweepResult = {
  scanned: number;
  translated: number;
  failed: number;
  backlog: boolean;
};

// items: partia wierszy do przetłumaczenia (już pobrana z DB z LIMIT).
// translateOne: tłumaczy+zapisuje jeden wiersz (rzuca przy błędzie).
// limitReached: czy DB zwróciło dokładnie LIMIT (czyli prawdopodobnie jest więcej).
export async function runTranslationSweep<T>(
  items: T[],
  translateOne: (item: T) => Promise<void>,
  opts: { limitReached?: boolean } = {}
): Promise<SweepResult> {
  let translated = 0;
  let failed = 0;
  for (const item of items) {
    try {
      await translateOne(item);
      translated++;
    } catch {
      failed++;
    }
  }
  return { scanned: items.length, translated, failed, backlog: opts.limitReached ?? false };
}
```

- [ ] **Step 4: PASS** (`npm test -- translation-sweep`)

- [ ] **Step 5: Commit**

```bash
git add sklep-meblowy/app/_lib/translation-sweep.ts sklep-meblowy/app/_lib/__tests__/translation-sweep.test.ts
git commit -m "feat(i18n): orkiestrator sweepa tłumaczeń + testy"
```

---

### Task 7: Serwis tłumaczeń + endpoint sweepa

**Files:**
- Create: `sklep-meblowy/app/_lib/translation-service.ts` (most: DB ↔ DeepL ↔ encje)
- Create: `sklep-meblowy/app/api/cron/translate/route.ts`
- Modify: `sklep-meblowy/.env.example` (+`DEEPL_API_KEY`)
- Modify: `sklep-meblowy/vercel.json` (+ cron)

- [ ] **Step 1: Serwis** — funkcje `translateProductById(id)`, `translatePendingBatch(limit)` używające `createAdminClient`, `translateTexts` jako `TranslateFn`, oraz `translate-entities`. Zapis `_de` + `needs_translation=false` + `translated_at=now()`.

```ts
// app/_lib/translation-service.ts
import { createAdminClient } from "./supabase/server";
import { translateTexts } from "./translate";
import { translateProductFields, type TranslateFn } from "./translate-entities";
import { runTranslationSweep } from "./translation-sweep";

const PRODUCTS_PER_RUN = 25;

const deepl: TranslateFn = (texts, opts) => translateTexts(texts, { html: opts?.html });

export async function translateProductRow(
  product: { id: string; name: string; description: string; color: string | null; material: string | null; description_sections: unknown },
  supabase: Awaited<ReturnType<typeof createAdminClient>>
): Promise<void> {
  const de = await translateProductFields(
    {
      name: product.name,
      description: product.description,
      color: product.color,
      material: product.material,
      description_sections: product.description_sections as never,
    },
    deepl
  );
  const { error } = await supabase
    .from("products")
    .update({
      name_de: de.name_de,
      description_de: de.description_de,
      color_de: de.color_de,
      material_de: de.material_de,
      description_sections_de: de.description_sections_de,
      needs_translation: false,
      translated_at: new Date().toISOString(),
    } as never)
    .eq("id", product.id);
  if (error) throw new Error(error.message);
}

// Pobiera partię produktów do tłumaczenia i przerabia. Sweep obejmuje TYLKO produkty,
// bo tylko one przychodzą hurtem (sync BL). Kategorie (kilka, edytowane ręcznie) i
// recenzje (pojedyncze przy submit) tłumaczone są inline (Task 8/13) — nie potrzebują
// sweepa. Ręczny „przetłumacz zaległe" w panelu pokrywa ewentualne braki.
export async function translatePendingProducts(limit = PRODUCTS_PER_RUN) {
  const supabase = await createAdminClient();
  const { data } = await supabase
    .from("products")
    .select("id, name, description, color, material, description_sections")
    .eq("needs_translation", true)
    .order("created_at", { ascending: true })
    .limit(limit);
  const items = (data ?? []) as { id: string; name: string; description: string; color: string | null; material: string | null; description_sections: unknown }[];
  return runTranslationSweep(items, (p) => translateProductRow(p, supabase), {
    limitReached: items.length === limit,
  });
}
```

- [ ] **Step 2: Endpoint** (auth jak `reconcile-bl` — reużyj `safeCompareSecret`)

```ts
// app/api/cron/translate/route.ts
import { NextResponse, type NextRequest } from "next/server";
import { safeCompareSecret } from "@/app/_lib/secure-compare";
import { translatePendingProducts } from "@/app/_lib/translation-service";

function isAuthorized(request: NextRequest, cronSecret?: string, syncSecret?: string): boolean {
  if (cronSecret && safeCompareSecret(request.headers.get("authorization"), `Bearer ${cronSecret}`)) return true;
  if (syncSecret && safeCompareSecret(request.headers.get("x-sync-secret"), syncSecret)) return true;
  return false;
}

export async function GET(request: NextRequest) {
  const cronSecret = process.env.CRON_SECRET;
  const syncSecret = process.env.BASELINKER_SYNC_SECRET;
  if (!cronSecret && !syncSecret) {
    return NextResponse.json({ error: "Brak CRON_SECRET i BASELINKER_SYNC_SECRET" }, { status: 500 });
  }
  if (!isAuthorized(request, cronSecret, syncSecret)) {
    return NextResponse.json({ error: "Nieautoryzowany" }, { status: 401 });
  }
  if (!process.env.DEEPL_API_KEY) {
    return NextResponse.json({ error: "Brak DEEPL_API_KEY" }, { status: 500 });
  }
  const products = await translatePendingProducts();
  console.log(`[translate] produkty: ${JSON.stringify(products)}`);
  return NextResponse.json({ products });
}
```

- [ ] **Step 3: `.env.example` + `vercel.json`**

`.env.example`: dopisz `DEEPL_API_KEY=`.
`vercel.json`: dodaj do `crons` wpis `{ "path": "/api/cron/translate", "schedule": "0 4 * * *" }` (Hobby: raz/dzień; częściej → zewnętrzny pinger z `x-sync-secret`, patrz spec).

- [ ] **Step 4: Typecheck + lint + test**

Run: `cd sklep-meblowy; node_modules\.bin\tsc.cmd --noEmit -p tsconfig.json; npm run lint; npm test`
Expected: exit 0; 100% testów PASS.

- [ ] **Step 5: Commit**

```bash
git add sklep-meblowy/app/_lib/translation-service.ts "sklep-meblowy/app/api/cron/translate/route.ts" sklep-meblowy/.env.example sklep-meblowy/vercel.json
git commit -m "feat(i18n): serwis tłumaczeń + endpoint sweepa /api/cron/translate"
```

---

### Task 8: Triggery write-path (flagowanie + inline)

**Files (modify):**
- `sklep-meblowy/app/_lib/baselinker-sync.ts` — przy upsercie nowego/zmienionego produktu ustaw `needs_translation: true` (NIE tłumacz inline — hurtowy sync → sweep).
- `sklep-meblowy/app/admin/produkty/actions.ts` — `updateProductBasics` i (jeśli osobna) zapis sekcji: po zapisie PL ustaw `needs_translation=true`, potem **best-effort inline** `translateProductRow` w `try/catch` (błąd nie wywala akcji).
- `sklep-meblowy/app/admin/produkty/[id]/actions` lub `updateProductDescriptionSections` — analogicznie.
- `sklep-meblowy/app/api/reviews/route.ts` — po insercie recenzji: best-effort inline tłumaczenie `comment` → `comment_de` (recenzje są inline-only — nie ma ich w sweepie).

- [ ] **Step 1:** W `baselinker-sync.ts` w obiekcie upsertu produktu dodać `needs_translation: true` (przy każdym sync, bo PL mogło się zmienić — `_de` i tak fallbackuje, a sweep dociągnie). Zachować istniejące pola.
- [ ] **Step 2:** W `updateProductBasics` po udanym `update(...)` PL dodać:

```ts
// best-effort inline DE (pojedynczy produkt z panelu) — błąd nie blokuje zapisu
try {
  const { createAdminClient } = await import("@/app/_lib/supabase/server");
  const { translateProductRow } = await import("@/app/_lib/translation-service");
  const sb = await createAdminClient();
  const { data: fresh } = await sb.from("products")
    .select("id, name, description, color, material, description_sections").eq("id", id).single();
  if (fresh) await translateProductRow(fresh as never, sb);
} catch (e) { console.warn("inline DE translate skipped:", e); }
```

(Analogicznie dla sekcji opisu i recenzji — wg tego samego wzorca, z `try/catch`.)

- [ ] **Step 3:** Typecheck + lint + test (exit 0 / PASS).
- [ ] **Step 4: Commit**

```bash
git add sklep-meblowy/app/_lib/baselinker-sync.ts sklep-meblowy/app/admin/produkty/actions.ts sklep-meblowy/app/api/reviews/route.ts
git commit -m "feat(i18n): flagowanie needs_translation w sync + inline DE w panelu (best-effort)"
```

---

## Faza C — Odczyt i render wg locale

### Task 9: Locale-aware reads (produkty/kategorie/recenzje/facets/search)

**Files (modify):** `app/_lib/products.ts`, `app/_lib/categories.ts`, `app/_lib/reviews.ts`, `app/_lib/search-filter.ts` (rozszerzenie), miejsca wołające.

Wzorzec: funkcje czytające dane przyjmują `locale` (z `getLocale()` w page/RSC) i mapują pola przez `pickLocalized`. Wyszukiwanie: gdy `locale==='de'`, `buildSearchOrFilter` buduje `.or()` po `name_de`/`description_de`.

- [ ] **Step 1: Rozszerz `buildSearchOrFilter`** o locale (test najpierw):

```ts
// dopisz do app/_lib/__tests__/search-filter.test.ts
import { buildSearchOrFilter } from "@/app/_lib/search-filter";
it("locale de → kolumny _de", () => {
  expect(buildSearchOrFilter("sofa", "de")).toBe("name_de.ilike.%sofa%,description_de.ilike.%sofa%");
});
it("locale pl (domyślnie) → kolumny PL", () => {
  expect(buildSearchOrFilter("sofa")).toBe("name.ilike.%sofa%,description.ilike.%sofa%");
});
```

Implementacja: dodaj 2. param `locale: 'pl'|'de' = 'pl'`; przy `de` użyj `name_de`/`description_de`. (Sanityzacja bez zmian.)

- [ ] **Step 2:** `getProducts`/`getProduct`/facets/`getReviewsForProduct`/`getCategories` przyjmują `locale` i zwracają `pickLocalized(...)` dla pól tekstowych; facets agregują `color_de`/`material_de` przy `de`. Strony (`/sklep`, `/produkt/[id]`, home) wołają `const locale = await getLocale()` i przekazują dalej.
- [ ] **Step 3:** Typecheck + lint + test (PASS, w tym nowe asercje search-filter).
- [ ] **Step 4: Commit** `feat(i18n): warstwa danych wg locale + wyszukiwanie DE`

---

### Task 10: Słownik UI (`dictionaries`)

**Files:** Create `app/_lib/dictionaries/pl.ts`, `app/_lib/dictionaries/de.ts`, `app/_lib/dictionaries/index.ts` (`t(key, locale)` / `getDictionary(locale)`); Test `__tests__/dictionaries.test.ts`.

- [ ] **Step 1: Test** — `getDictionary('de').nav.shop === 'Shop'`; klucz brakujący w DE → fallback do PL.
- [ ] **Step 2: Implementacja** — typowany obiekt PL (źródło kluczy), DE jako `Partial<>` z fallbackiem do PL w `t()`. Zacznij od kluczy faktycznie używanych (nav, przyciski koszyka/produktu, puste stany, checkout-UI) — wypełniaj wraz z Task 11.
- [ ] **Step 3: PASS** + **Step 4: Commit** `feat(i18n): słownik UI PL/DE + t()`

---

### Task 11: Render wg locale (layout, switcher, komponenty)

**Files (modify):** `app/layout.tsx` (`<html lang>` dynamiczne wg `getLocale()`), `app/_components/layout/Navbar.tsx`/`TopBar.tsx` (przełącznik), `ProductCard`, `ProductMainSection`, `ProductDescriptionSections`, `ReviewList`, breadcrumby, Footer — `t()` dla UI + pola zlokalizowane dla treści.

- [ ] **Step 1: `<html lang>`** — w root layout: `const locale = await getLocale(); <html lang={locale}>`.
- [ ] **Step 2: `LanguageSwitcher`** (client): czyta `usePathname()`, linkuje do `localizePath(stripLocale(path).pathname, otherLocale)` zachowując query. Wstaw w Navbar/TopBar.
- [ ] **Step 3:** Komponenty produktu/recenzji/kategorii renderują `pickLocalized(...)`/`t(...)` (locale z propsa przekazanego z RSC). Ceny: `toLocaleString(locale==='de'?'de-DE':'pl-PL')`.
- [ ] **Step 4:** Typecheck + lint; smoke-test `/de/sklep` i `/de/produkt/<id>` — UI DE, treść DE jeśli przetłumaczona, inaczej PL.
- [ ] **Step 5: Commit** `feat(i18n): render wg locale (html lang, przełącznik, komponenty)`

---

## Faza D — SEO + Admin

### Task 12: SEO — hreflang + sitemap DE

**Files:** Create `app/_lib/sitemap-i18n.ts` (pure filtr/builder + test); Modify `app/sitemap.ts`, `app/produkt/[id]/page.tsx` (`generateMetadata.alternates`), `app/layout.tsx` (default alternates), `app/sklep/page.tsx`.

- [ ] **Step 1: Test pure-helpera** `productDeAlternate(product, base)` → zwraca wpis DE tylko gdy `needs_translation===false`; w przeciwnym razie `null`. Oraz `alternatesFor(pathname, base, { hasDe })` → `{ languages: { pl, de?, 'x-default' } }`.
- [ ] **Step 2: Implementacja helpera** wg testu.
- [ ] **Step 3: sitemap.ts** — dla każdego produktu dodaj wpis `/de/produkt/{id}` tylko gdy `needs_translation=false`; statyczne home/sklep + `/de/`+`/de/sklep`; każdy wpis z `alternates: { languages: { pl, de? } }`. (Strony info/prawne BEZ DE — etap ③.)
- [ ] **Step 4: generateMetadata** (produkt + sklep + layout): `alternates: { canonical: <self>, languages: alternatesFor(...) }`; pomiń `de` gdy brak tłumaczenia. `openGraph.locale` wg locale. Meta title/description z pól zlokalizowanych.
- [ ] **Step 5:** Typecheck + lint + test (PASS) + smoke-test `view-source` `/de/produkt/<id>` (hreflang obecny). **Commit** `feat(seo): hreflang + sitemap DE (tylko przetłumaczone)`

---

### Task 13: Panel — override DE + status + retranslate

**Files (modify):** `app/admin/produkty/[id]/*` (sekcja DE: edycja `name_de`/`description_de`/sekcje + „Przetłumacz ponownie"), `app/admin/kategorie/KategorieEditor.tsx` (`label_de`), `app/admin/baselinker/BaseLinkerSyncPanel.tsx` (licznik zaległych + „przetłumacz zaległe"); actions: zapis ręcznego DE ustawia `needs_translation=false`; „Przetłumacz ponownie" ustawia `needs_translation=true` i woła inline serwis.

- [ ] **Step 1:** Akcja `retranslateProduct(id)` (`requireAdmin`): `needs_translation=true` → `translateProductRow` inline (best-effort) → zwróć status.
- [ ] **Step 2:** Akcja `saveProductDe(id, fields)` (`requireAdmin`): zapis `_de` + `needs_translation=false`.
- [ ] **Step 3:** UI: sekcja DE w edytorze produktu (wzorzec override jak `DescriptionSectionsEditor`); pole `label_de` w kategoriach; w panelu BL przycisk + licznik (`select count where needs_translation=true`). Status per produkt: „przetłumaczone / oczekuje".
- [ ] **Step 4:** Typecheck + lint + test (PASS). **Commit** `feat(i18n): panel — override DE, status, przetłumacz ponownie`

---

## Po wdrożeniu (deploy etapu ①)

- Migracja 29 na Supabase; env `DEEPL_API_KEY` (+ istniejące `CRON_SECRET`/`BASELINKER_SYNC_SECRET`) na Vercel; cron `/api/cron/translate` w `vercel.json` aktywny (lub pinger).
- Jednorazowo: „przetłumacz zaległe" dla istniejącego katalogu (wszystkie mają `needs_translation=true` po migracji).
- Smoke-test: `/de`, `/de/sklep`, `/de/produkt/<id>` (UI+treść DE, fallback PL gdzie brak), przełącznik języka, hreflang w źródle, sitemap zawiera `/de/...` tylko dla przetłumaczonych.
- DeepL Free: konto + `DEEPL_API_KEY` (rejestracja wymaga karty — bez obciążeń w limicie 500k/mc).

## Ryzyka

1. **proxy.ts w Next 16** — gdyby rewrite/matcher nie działał jak w docs, zweryfikować `node_modules/next/dist/docs/.../proxy.md`. To jedyny punkt niepewności mechanizmu.
2. **Kształt `ProductDescriptionSection`** — dopasować w Task 5 do `app/_lib/types.ts`.
3. **Wolumen DeepL** — przy meblach śladowy; sweep ma LIMIT + `backlog`.
